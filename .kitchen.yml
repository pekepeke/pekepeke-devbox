---
driver:
  name: docker

provisioner:
  name: ansible_playbook
  hosts: localhost
  playbook: playbook.yml

platforms:
  - name: ubuntu-14.04
    driver_config:
      image: ubuntu:14.04

suites:
  - name: default

